# Provisioner

This crate contains a set of tools that can be useful for provisioning testing environments:

1. DB seeding CLI
2. Provisioning server, which seeds the DB and creates connect tokens which can be used for simulation

For documentation, please run:

```bash
cargo run -- --help
```

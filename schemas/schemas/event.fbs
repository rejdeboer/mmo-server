include "common.fbs";

namespace mmo;

table EntityMoveEvent {
	entity_id: uint32;
	position: Vec3; 
	direction: Vec2;
}

union EventData {
	EntityMoveEvent
}

table Event {
	data: EventData (required);
}

table BatchedEvents {
	// server_tick: uint;
	events: [Event];
}

root_type BatchedEvents;

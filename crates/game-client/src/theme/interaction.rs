use bevy::prelude::*;

/// Plugin for shared interaction behaviors (currently a placeholder for
/// future hover/press effects that are shared across all UI widgets).
pub(super) fn plugin(_app: &mut App) {
    // Future: register shared interaction observers here
}
